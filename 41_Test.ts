

let megician : string [] = ["Harry Porter", "Hermione Granger", "Ron Weasley", "Albus Dumbledore"];

function  show_megician (megician:string []) {

    megician.forEach(element => {
        console.log(element);
    }); 
    
}

show_megician(megician)
