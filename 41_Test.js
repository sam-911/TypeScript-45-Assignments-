let megician = ["Harry Porter", "Hermione Granger", "Ron Weasley", "Albus Dumbledore"];
function show_megician(megician) {
    megician.forEach(element => {
        console.log(element);
    });
}
show_megician(megician);
export {};
